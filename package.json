{
  "name": "SwapTrade",
  "author": "SwapTrade",
  "version": "1.0.0",
  "description": "SwapTrade",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager --overwrite ./ SwapTrade --out ./release --platform=win32 --arch=x64 --overwrite",
    "build-all": "electron-packager --overwrite ./ SwapTrade --out ./release --all --overwrite"
  },
  "dependencies": {
    "jquery": "^2.1.4",
    "react": "^0.14.3",
    "react-chartist": "^0.10.1",
    "react-dom": "^0.14.3",
    "react-router": "0.13"
  },
  "devDependencies": {
    "reactify": "^1.1.1",
    "watchify": "^3.6.1",
    "browserify": "^12.0.1",
    "electron": "^1.7.8",
    "electron-packager": "^9.1.0"
  },
  "browserify": {
    "transform": [
      "reactify"
    ]
  }
}
